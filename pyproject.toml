[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "cultural-wordle"
version = "0.1.0"
description = "A Wordle solver driven by a genuine cultural algorithm."
requires-python = ">=3.11"
